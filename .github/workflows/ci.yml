name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

env:
  GO_VERSION: "1.26"

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
      - uses: golangci/golangci-lint-action@v6
        with:
          # `.golangci.yml` uses the v2 config schema (top-level `version`,
          # `formatters`, `linters.default`) — golangci-lint-action's
          # `version: latest` resolves to the latest v1.x, which rejects
          # that schema outright ("additional properties ... not allowed").
          # Pin to a real v2 release explicitly instead.
          version: v2.13.2
          # golangci-lint's prebuilt binaries lag the newest Go releases and
          # refuse to lint a module targeting a Go language version newer
          # than the one they were built with (a real failure hit on the
          # first CI run against go.mod's `go 1.26.6`). Building from source
          # with the toolchain actions/setup-go just installed sidesteps
          # that version skew entirely.
          install-mode: goinstall

  test:
    name: Unit tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
      - run: go build ./...
      - run: go vet ./...
      - run: gofmt -l . | tee /tmp/gofmt.out && [ ! -s /tmp/gofmt.out ]
      - run: go test ./... -race -coverprofile=coverage.out
      - uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: coverage.out

  test-integration:
    name: Integration tests (real Postgres via testcontainers)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
      # ubuntu-latest runners come with Docker pre-installed, which is all
      # testcontainers-go needs to spin up real Postgres containers.
      - run: go test -tags=integration ./... -v

  vulncheck:
    name: Vulnerability scan (govulncheck)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
      - run: go install golang.org/x/vuln/cmd/govulncheck@latest
      - run: govulncheck ./...

  build:
    name: Docker build, image scan, SBOM
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - name: Build image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: false
          load: true
          tags: taskflow-api:ci
          build-args: |
            VERSION=${{ github.sha }}
      - name: Scan image for vulnerabilities (Trivy)
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: taskflow-api:ci
          format: table
          exit-code: "0" # report only; flip to "1" once a baseline is triaged
          severity: "CRITICAL,HIGH"
      - name: Generate SBOM
        uses: anchore/sbom-action@v0
        with:
          image: taskflow-api:ci
          format: spdx-json
          output-file: sbom.spdx.json
      - uses: actions/upload-artifact@v4
        with:
          name: sbom
          path: sbom.spdx.json
      # No registry configured yet, so the image is built and scanned but
      # not pushed anywhere — add a `docker/login-action` + push step once
      # one exists.
